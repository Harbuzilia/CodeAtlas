# Repository Map (refreshed 2026-04-14)

Scope: `D:\AllMyProject\CodeAtlas\Opencode1`
Purpose: compact architecture map for `contextscout` and runtime debugging.
Status: generated summary artifact; replace when runtime structure changes materially.

## Runtime Source of Truth
- `opencode.json` — agent registry, permissions, default agent
- `agents/openagent.md` — orchestration, routing, one-shot and planner thresholds
- `context/core/workflows/delegation.md` — canonical delegation workflow
- `context/project/patterns.md` — project-specific runtime invariants
- `PROJECT_GUIDE.md` — human-facing runtime guide

## Key Directories
```
agents/
  openagent.md        primary router
  contextscout.md     read-only context discovery
  coder.md            implementation + TDD execution
  tester.md           test-focused execution
  debugger.md         build/runtime remediation
  reviewer.md         read-only review
  planner.md          decomposition for 4+ file tasks
  externalscout.md    external docs via Context7
  docwriter.md        documentation updates
  uitester.md         Chrome DevTools UI checks

context/
  navigation.md
  index.md
  core/
    config/paths.json
    standards/{code,tests,docs}.md
    workflows/{delegation,review}.md
  project/{navigation,patterns}.md

skills/
  ast-index/
  repomap/
  context7/
  test-driven-development/
  single-flow-task-execution/
  verification-before-completion/
  writing-plans/
  ...other runtime skills

scripts/
  install.mjs
  validate-registry.mjs
  validate-context-refs.mjs
  validate-frontmatter-sync.mjs

.opencode/
  bin/ast-index.exe
  repomap.txt
  task_state.md
```

## Current Runtime Contracts
- Planner threshold: `4+ files` is canonical across runtime docs.
- Registry-managed agent files require frontmatter `id` matching `registry.json`.
- Legacy runtime references such as `subagents/reviewer` and `subagents/research/externalscout` are forbidden.
- `ast-index` is a bundled project-local binary at `.opencode/bin/ast-index.exe`.
- `repomap.txt` is supportive context only; behavior truth remains code + validators.

## Validation Entry Points
- `npm run validate:all`
- `npm run validate:runtime`
- `npm run smoke:functional`
- `node --test tests/*.test.mjs`

## Quick Navigation Hints
- Routing issue -> `agents/openagent.md`, `context/core/workflows/delegation.md`
- Frontmatter / registry drift -> `scripts/validate-frontmatter-sync.mjs`, `registry.json`
- Installer / bundled tool behavior -> `scripts/install.mjs`, `skills/ast-index/SKILL.md`
- Runtime invariants -> `validate-runtime-governance.mjs`, `PROJECT_GUIDE.md`
